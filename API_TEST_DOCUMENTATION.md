# 📋 توثيق اختبار الـ APIs

## 📌 نظرة عامة

تم إنشاء مجموعة شاملة من الاختبارات للتحقق من صحة عمل جميع الـ APIs المستخدمة في مشروع موريتانيا ميتيو.

---

## 🧪 الاختبارات المتاحة

### 1. **اختبار Open-Meteo API** ✅

#### الغرض:
التحقق من الاتصال بـ Open-Meteo API وجلب بيانات الطقس لنواكشوط

#### ما يتم فحصه:
- ✓ الاتصال بـ API يتم بنجاح
- ✓ العنوان الصحيح: `https://api.open-meteo.com/v1/forecast`
- ✓ إحداثيات نواكشوط (lat: 18.0735, lon: -15.9582)
- ✓ جلب البيانات الكاملة

#### البيانات المرتجعة:
```json
{
  "current": {
    "temperature_2m": 25.5,
    "weather_code": 0,
    "wind_speed_10m": 12.3,
    "relative_humidity_2m": 65,
    "pressure_msl": 1013.25
  },
  "hourly": { ... },
  "daily": { ... }
}
```

---

### 2. **اختبار البيانات الحالية (Current)** 🌡️

#### الغرض:
التحقق من وجود جميع معاملات البيانات الحالية

#### المعاملات المفحوصة:
1. `temperature_2m` - درجة الحرارة بالدرجة المئوية
   - النوع: `number`
   - المثال: `25.5`

2. `weather_code` - رمز الطقس
   - النوع: `number`
   - المثال: `0` (صافي), `61` (مطر خفيف)

3. `wind_speed_10m` - سرعة الريح على ارتفاع 10 متر
   - النوع: `number`
   - الوحدة: `km/h`
   - المثال: `12.3`

4. `relative_humidity_2m` - الرطوبة النسبية على ارتفاع 2 متر
   - النوع: `number`
   - الوحدة: `%`
   - النطاق: `0-100`

5. `pressure_msl` - الضغط الجوي
   - النوع: `number`
   - الوحدة: `hPa`
   - المثال: `1013.25`

---

### 3. **اختبار البيانات الهورية (Hourly)** ⏱️

#### الغرض:
التحقق من وجود بيانات الساعات القادمة

#### المعاملات المفحوصة:
1. `time[]` - مصفوفة الأوقات بصيغة ISO 8601
   - المثال: `["2024-01-15T00:00", "2024-01-15T01:00", ...]`

2. `temperature_2m[]` - درجات الحرارة للساعات
   - المثال: `[22.5, 21.3, 20.8, ...]`

3. `precipitation_probability[]` - احتمالية الهطول بالنسبة المئوية
   - المثال: `[0, 5, 10, ...]`

---

### 4. **اختبار البيانات اليومية (Daily)** 📅

#### الغرض:
التحقق من وجود توقعات الطقس اليومية

#### المعاملات المفحوصة:
1. `time[]` - مصفوفة التواريخ
2. `weather_code[]` - أكواد الطقس اليومية
3. `temperature_2m_max[]` - أقصى درجة حرارة يومية
4. `temperature_2m_min[]` - أقل درجة حرارة يومية
5. `precipitation_sum[]` - مجموع الأمطار اليومية (mm)
6. `wind_speed_10m_max[]` - أقصى سرعة ريح يومية

---

### 5. **اختبار Supabase Connection** 🗄️

#### الغرض:
التحقق من تكوين Supabase والدوال المتوقعة

#### ما يتم فحصه:
✓ وجود العميل (`supabase` client)
✓ الدوال المعرفة:
  - `testConnection()` - اختبار الاتصال
  - `addFavoriteCity()` - إضافة مدينة للمفضلة
  - `getFavoriteCities()` - جلب المدن المفضلة
  - `removeFavoriteCity()` - حذف مدينة من المفضلة
  - `addSearchHistory()` - إضافة بحث للسجل
  - `getSearchHistory()` - جلب سجل البحث
  - `getActiveAlerts()` - جلب التنبيهات النشطة

#### الجداول المتوقعة:
- `favorites` - المدن المفضلة (user_id, city_name)
- `search_history` - سجل البحث (user_id, query, created_at)
- `alerts` - التنبيهات (created_at وغيرها)

---

### 6. **اختبار CORS** 🔐

#### الغرض:
التأكد من عدم وجود مشاكل CORS (Cross-Origin Resource Sharing)

#### ما يتم فحصه:
- ✓ السماح بطلبات من جميع المصادر
- ✓ عدم وجود أخطاء CORS
- ✓ دعم الطلبات preflight

---

## 🚀 كيفية تشغيل الاختبارات

### الطريقة 1: اختبار تفاعلي في المتصفح
1. افتح ملف `test-apis.html` في المتصفح
2. اضغط على أزرار الاختبار للفحص الفردي
3. أو اضغط على "🚀 شغل جميع الاختبارات"
4. اعرض النتائج والملخص

### الطريقة 2: اختبارات Vitest
```bash
npm run test -- src/api.test.js
# أو لتشغيل جميع الاختبارات
npm run test

# عرض واجهة المستخدم
npm run test:ui

# تقرير التغطية
npm run test:coverage
```

### الطريقة 3: اختبار Node.js مباشر
```bash
node test-apis.js
```

---

## ✅ معايير النجاح

### ✓ Open-Meteo API يجب أن:
- يرجع status code 200
- يتضمن بيانات حالية (`current`)
- يتضمن بيانات هورية (`hourly`)
- يتضمن بيانات يومية (`daily`)
- يرجع جميع المعاملات المطلوبة

### ✓ البيانات يجب أن:
- تكون من النوع الصحيح (number, array, object)
- تحتوي على قيم صحيحة
- لا تكون `null` أو `undefined`
- تكون في النطاق المتوقع (مثلاً: 0-100 للنسبة المئوية)

### ✓ Supabase يجب أن:
- يكون مهيأ بشكل صحيح
- توجد جميع الدوال المطلوبة
- يكون جاهز للعمل (حتى بدون مفاتيح صحيحة)

### ✓ CORS يجب أن:
- لا يحجب الطلبات من المتصفح
- يسمح بـ OPTIONS requests
- يسمح بـ GET requests

---

## 📊 نتائج الاختبارات

### التحقق من النتائج:
```
✅ Open-Meteo API: ✓ ناجح
✅ البيانات الحالية: ✓ ناجح  
✅ البيانات الهورية: ✓ ناجح
✅ البيانات اليومية: ✓ ناجح
✅ Supabase: ✓ معرف بشكل صحيح
✅ CORS: ✓ لا توجد مشاكل
```

---

## 🔧 استكشاف المشاكل

### مشكلة: خطأ في الاتصال بـ Open-Meteo
**الحل:**
- تحقق من الاتصال بالإنترنت
- تحقق من صحة العنوان: `https://api.open-meteo.com/v1/forecast`
- جرب الطلب مباشرة في المتصفح

### مشكلة: بيانات مفقودة
**الحل:**
- تحقق من معاملات الـ query string
- تأكد من شمول جميع المعاملات المطلوبة
- جرب بدون الفلاتر الإضافية أولاً

### مشكلة: خطأ CORS
**الحل:**
- تأكد من أن الطلب يأتي من أصل موثوق
- استخدم proxy إن لزم الأمر
- تحقق من رؤوس الطلب

### مشكلة: Supabase غير معرف
**الحل:**
- تحقق من ملف `.env.local`
- أضف متغيرات البيئة المطلوبة
- أعد تحميل التطبيق

---

## 📝 معلومات إضافية

### فترات الاستجابة المتوقعة:
- Open-Meteo API: 100-500ms
- البيانات الهورية: 200-600ms (كميات أكبر)
- البيانات اليومية: 100-300ms

### الحد الأقصى للطلبات:
- Open-Meteo: حد مجاني 10,000 طلب/يوم
- لا يتطلب مفتاح API

### المتصفحات المدعومة:
- Chrome/Edge (آخر إصدار)
- Firefox (آخر إصدار)
- Safari (آخر إصدار)

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من ملف `TROUBLESHOOTING.md`
2. عرّج الأخطاء في `errorLogger.js`
3. تواصل مع فريق التطوير

---

## 🎯 خطوات التحقق النهائية

قبل الإطلاق النهائي:
- [ ] جميع اختبارات Open-Meteo تمر بنجاح
- [ ] البيانات الحالية والهورية واليومية موجودة
- [ ] Supabase مهيأ ويعمل
- [ ] لا توجد أخطاء CORS
- [ ] الأداء مقبول (< 1 ثانية للاستجابة)
- [ ] معالجة الأخطاء تعمل بشكل صحيح

✅ **عند تحقق جميع النقاط: APIs جاهزة للاستخدام في الإنتاج!**
